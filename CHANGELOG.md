# CHANGELOG

<!-- markdownlint-disable MD024 -->

## [Unreleased]

---

## [v14.5] — 2026-05-03

### Исправлено

- Доверительные интервалы систематически смещались ниже точечного прогноза (~45 RUB в narrow-режиме).
  Причина: квантильные модели обучались предсказывать абсолютную цену `Close` из `[lstm_pred, xgb_pred]`.
  Поскольку SBER большую часть обучающего окна торговался ниже текущих уровней, 75-й перцентиль
  цен оказывался вблизи точечного прогноза, а 25-й — на 40–45 RUB ниже.
  Промежуточная попытка (in-sample остатки) дала CI 4–5 RUB с прогнозом вне интервала —
  мета-лернер слишком хорошо подогнан на обучающей выборке.
  Итоговое решение: квантильные модели обучаются на OOS-остатках (`actual − meta_pred`)
  с тестовых окон всех трёх walk-forward сплитов; CI добавляется к точечному прогнозу
  как смещение: `lower = pred + residual_q25`, `upper = pred + residual_q75`.
  Результат: CI 5–9 RUB, все горизонты ✓, интервал симметричен вокруг прогноза.

---

## [v14.4] — 2026-04-29

### Добавлено

- `stock_modelv14.py` — сводная таблица `FORECAST SUMMARY` в основном логе:
  для каждого горизонта (+1d/+2d/+3d) выводятся дата, прогноз, RMSE, MAE, R²,
  границы CI [lower – upper], ширина CI и флаг ✓/✗ (входит ли точечный прогноз в интервал)

### Исправлено

- `.txt`-отчёт (`{TICKER}_forecast_v14_{timestamp}.txt`) — метрики теперь per-horizon
  (ранее в файл записывались только метрики h=1; h=2 и h=3 отсутствовали)

---

## [v14.3] — 2026-04-29

### Исправлено

- CI-bounds систематически оказывались ниже точечного прогноза.
  Причина: квантильные модели обучались на `X_train_flat` (1020 признаков),
  а мета-лернер — на `[lstm_pred, xgb_pred]` (2 признака); при предсказании
  CI передавался `current_scaled_flat` вместо `meta_input`.
  Исправление: квантильные модели обучаются после мета-лернера на тех же
  `meta_train`-фичах и предсказывают с `meta_input`.

### Документация

- `architecture.md` — актуализирован под v14: счётчик признаков 34,
  форма входа LSTM `(30, 34)`, XGBoost flat `1020`, описание Direct
  Multi-Step вместо авторегрессии, квантильные модели на meta-features,
  добавлена строка вопроса 5 (Режим CI), `XGBOOST_DEVICE` в таблице конфига,
  удалена `scipy`, обновлена версия `xgboost` до 3.x+, добавлен раздел «Тесты»
- `improvements.md` — добавлен пункт 3a (исправление CI-архитектуры)
- `.gitignore` — добавлен `docs/superpowers/`

---

## [v14.2] — 2026-04-28

### Добавлено

- `XGBOOST_DEVICE: str = 'cpu'` в `config.py` — переключение XGBoost на GPU одной строкой (`'cuda'`)
- Параметр `device` передаётся во все `XGBRegressor`: Optuna, основная модель, квантильные модели, meta-learner

### Причина

RTX 5070 Ti (Blackwell/GB203, 16 GB VRAM) поддерживает XGBoost CUDA-ускорение начиная
с CUDA 12.8 и XGBoost 2.0+. При обучении 54 деревьев (18 × 3 горизонта) GPU даёт
заметный прирост скорости. TensorFlow 2.11+ не поддерживает GPU на Windows нативно —
`XGBOOST_DEVICE` охватывает только XGBoost-часть ансамбля.

---

## [v14.1] — 2026-04-28

### Добавлено

- Выбор режима доверительных интервалов (`ci_mode`):
  - **Широкий** (`wide`, по умолчанию) — 5/95 перцентили, полная история обучения
  - **Узкий** (`narrow`) — 25/75 перцентили, последние 756 торговых дней (~3 года)
- Интерактивный вопрос `5.` в `get_user_inputs()`
- CLI-флаг `--ci-mode wide|narrow`
- Helper `_get_ci_params(ci_mode, X_train, y_train)` инкапсулирует логику выбора

### Исправлено

- Прогноз начинался со следующего дня после даты запуска, пропуская текущий торговый день.
  Причина: `base_date = end_date` (сегодня), но MOEX возвращает данные только до вчера.
  Теперь `base_date = data['Date'].max()` — последняя дата в фактических данных.
  Прогноз day 1 теперь = следующий рабочий день после последней известной цены (без пропуска).

### Причина

Широкий CI (5/95 на полной истории) учитывал кризис SBER 2022 и давал нижнюю границу
~260 при цене ~320 — статистически верно, но практически неинформативно.
Узкий режим использует актуальную волатильность последних 3 лет.

---

## [v14.0] — 2026-04-27

### Изменено (архитектурно)

- `stock_modelv13.py` переименован в `stock_modelv14.py` — архитектурный мейлстоун
- `prepare_and_train_model` — авторегрессивный прогноз на 2–3 дня заменён прямыми независимыми моделями на каждый горизонт (Direct Multi-Step Forecasting / MIMO):
  - Добавлен параметр `horizon: int = 1`
  - При подготовке обучающих последовательностей цель сдвигается: `y[i] = Close[i + horizon]`; верхняя граница цикла `len(scaled_df) - horizon`
  - Прогнозный блок: одно прямое предсказание из текущих данных вместо авторегрессивного цикла; синтетические строки Open/High/Low/Volume удалены
  - В `main` и `run_backtest` функция вызывается трижды (horizon=1,2,3); результаты мержатся в словари `forecasts` и `confidence_intervals`
  - Итого моделей: 54 (18 на горизонт × 3) вместо 18

### Причина

Авторегрессивный подход накапливал ошибку: для прогноза на день 2 и 3 модель получала синтетические Open/High/Low/Volume, а технические индикаторы (RSI, ATR, Bollinger Bands и др.) пересчитывались на основе этих фиктивных значений. Ошибка дня 1 входила в признаки дня 2, ошибка дня 2 — в признаки дня 3. Доверительные интервалы для горизонтов 2 и 3 были статистически несостоятельны.

### Плюсы нового подхода

- Нет накопления ошибки — каждый горизонт предсказывается независимо из реальных данных
- Честные CI — квантильные модели каждого горизонта обучены на соответствующем сдвиге цели
- Академически обоснован: Direct Multi-Step / MIMO forecasting — стандарт для горизонтов > 1
- Интерпретируемые метрики: RMSE/MAE/R² отдельные для каждого горизонта (показывают деградацию качества с ростом горизонта)
- Внешний интерфейс (`forecasts[h][-1]`, `confidence_intervals[h]`) не изменился — код вывода не тронут

### Минусы / компромиссы

- Время обучения ×3 (54 модели вместо 18)
- Три независимых ансамбля без разделяемого представления между горизонтами

---

## [v13.6] — 2026-04-22 02:30

### Добавлено

- `stock_modelv13.py` — 5 новых признаков на основе доходностей (returns):
  - `Vol_Return_5`, `Vol_Return_10`, `Vol_Return_20` — скользящее стандартное отклонение дневных доходностей за 5/10/20 дней (волатильность режима)
  - `Return_MA_5`, `Return_MA_10` — скользящее среднее дневных доходностей за 5/10 дней (направление momentum на returns, не на ценах)
- Вектор признаков расширен с 29 до 34; оба определения `features` (основное и Optuna) обновлены
- `update_technical_indicators_single_row` обновлена: поддерживает вычисление новых признаков для авторегрессивного прогноза на 2–3 дня

### Причина

`SMA_10`/`EMA_20` работают на уровнях цен и коррелированы с масштабом. Признаки на returns захватывают:

- **Кластеризацию волатильности** (GARCH-эффект): высоковолатильные периоды сгруппированы — сигнал для модели о режиме рынка
- **Momentum на returns**: средняя доходность за период отражает тренд независимо от ценового уровня

Изменение следует из анализа ACF/PACF в отчёте по производственной практике.

---

## [v13.5] — 2026-04-22 02:30

### Исправлено

- `stock_modelv13.py` — `verify=False` заменён на `verify=certifi.where()` в обоих вызовах Tinkoff Invest API; добавлен `import certifi`; устранена MITM-уязвимость и `InsecureRequestWarning`

### Добавлено

- `requirements.txt` — зафиксированы 11 прямых зависимостей с точными версиями: `tensorflow`, `xgboost`, `optuna`, `pandas`, `numpy`, `scikit-learn`, `matplotlib`, `requests`, `certifi`, `moexalgo`, `python-dotenv`

### Причина

SSL-верификация была отключена для всех HTTPS-соединений с Tinkoff API, что делало их уязвимыми к перехвату. `certifi` уже присутствует в окружении как транзитивная зависимость `requests`. `requirements.txt` закрывает пункт воспроизводимости окружения.

---

## [v13.4] — 2026-04-22 01:46

### Исправлено

- `architecture.md` — счётчик признаков исправлен с 39 на 29 во всех четырёх местах (диаграмма, описание скейлера, нотация формы, раздел потока данных); количество фундаментальных с 17 до 7
- `architecture.md` — блоки ASCII-диаграмм помечены как `text` (устранено предупреждение MD040)
- `improvements.md` — удалена дублирующая «Сводная таблица», убраны потерявшиеся строки

### Добавлено

- `.vscode/settings.json` — путь к интерпретатору venv312 и подавление ложных предупреждений Pylance для TensorFlow/Keras (`reportMissingModuleSource`, `reportAttributeAccessIssue`, `reportOperatorIssue`)

### Удалено

- `stock_modelv13.py` — неиспользуемые импорты: `TensorBoard`, `ModelCheckpoint`, `interp1d`, `io`, `Path`

### Причина

Неиспользуемые импорты замусоривали пространство имён и генерировали предупреждения линтера. Счётчик признаков в документации не соответствовал фактическому после отключения нулевых фундаментальных фич. VS Code выдавал ложные ошибки на весь TF-модуль из-за отсутствия стабов Pylance.

---

## [v13.3] — 2026-04-22 01:33

### Исправлено

- `stock_modelv13.py` — даты прогноза теперь сдвигаются на рабочие дни (`next_business_day`): прогноз на пятницу больше не указывает субботу как торговый день

### Изменено

- `stock_modelv13.py` — нулевые фундаментальные признаки закомментированы в обоих определениях `features`; активные (из Tinkoff API) сгруппированы отдельно

### Причина

Даты прогноза некорректно указывали выходные дни. Нулевые признаки убраны из вектора фич, чтобы не передавать модели шумовые константы. Комментарии с TODO указывают на `financemarker.ru` как источник для заполнения отключённых признаков.

---

## [v13.2] — 2026-04-22 00:53

### Исправлено

- `stock_modelv13.py` — ошибка отступа: блок `if backtest_mode / else` перенесён внутрь `if __name__ == '__main__':`; ранее код выполнялся на уровне модуля, что ломало импорт
- `stock_modelv13.py` — Optuna для XGBoost теперь оценивает качество на holdout-выборке (20% от train), а не на тех же данных, на которых обучает; устранено in-sample переобучение при подборе гиперпараметров

### Добавлено

- `stock_modelv13.py` — функция `get_usd_rub_rate()`: актуальный курс USD/RUB загружается с API ЦБ РФ вместо захардкоженного значения 82.91; при недоступности API используется fallback 90.0

### Причина

Исправление структурного бага и некорректной оценки в Optuna напрямую влияют на воспроизводимость и качество результатов модели. Динамический курс устраняет дрейф признака `value_usd` при изменении курса рубля.

---

## [v13.1] — 2026-04-22 00:26

### Добавлено

- `config.py` — все гиперпараметры LSTM и XGBoost вынесены из `.env` в Python-конфиг; безопасно коммитить
- `.gitignore` — исключены `.env`, директория `outputs/`, артефакты Python и IDE
- `CHANGELOG.md` — этот файл
- `architecture.md` — описание архитектуры проекта, потока данных, переменных окружения и порядка запуска
- `improvements.md` — анализ плюсов/минусов текущего решения и рекомендации

### Изменено

- `stock_modelv13.py` — гиперпараметры теперь импортируются из `config.py` вместо чтения через `os.getenv`
- `.env` — оставлены только API-токены (секреты); параметры моделей перемещены в `config.py`
- `README.md` — создан по образцу проектной документации

### Причина

Разделение секретов (токены) и конфигурации (гиперпараметры): токены не должны попадать в VCS, гиперпараметры — наоборот, полезно версионировать для воспроизводимости экспериментов.

---

## [v13.0] — 2026-04-21 23:48

### Добавлено

- Стэкинг-ансамбль LSTM + XGBoost + Meta-Learner
- Квантильная регрессия для доверительных интервалов (α = 0.05, 0.50, 0.95)
- Walk-forward валидация (3 сплита с расширяющимся окном)
- Оптимизация гиперпараметров через Optuna с персистентностью результатов
- Режим бэктеста: обучение до заданной даты, сравнение с реальными ценами
- Загрузка данных MOEX через `moexalgo` + fallback на прямой ISS REST API
- Обогащение фундаментальными данными через T-Bank Invest API
- 39 признаков: OHLCV + 17 технических индикаторов + 17 фундаментальных
- Интерактивный режим ввода параметров и CLI-режим через `argparse`

---

## История проекта: v1 – v13 (до рефакторинга)

> Исторические записи перенесены из `oldCHANGELOG.md`.
> Версии v1–v12 существовали как отдельные файлы (`stock_model.py`, `stock_modelv8.py` и т.д.);
> начиная с v13.0 проект переведён на семантическое версионирование в рамках одного файла.

---

## [v13] — январь 2026

**Файл:** `stock_modelv13.py`

### Изменения

- `LSTM_LOOK_BACK` увеличен до 30 (было 20)
- `LSTM_UNITS` увеличен до 64 (было 50)
- `XGBOOST_MAX_DEPTH` = 6 — оптимальный баланс точности и скорости
- Полная интеграция Optuna + `TinkoffFundamentalLoader` из v11–v12
- JSON-логирование всех метрик обучения

---

## [v12 / v12v1] — стандартизация конфигурации

**Файлы:** `stock_modelv12.py`, `stock_modelv12v1.py`

### Изменения

- Все гиперпараметры вынесены в `.env` — нет жёстко заданных значений
- Новые переменные окружения: `LSTM_LOOK_BACK`, `LSTM_EPOCHS`, `LSTM_PATIENCE`, `LSTM_BATCH_SIZE`, `LSTM_LEARNING_RATE`, `LSTM_DROPOUT_RATE`, `LSTM_UNITS`; `XGBOOST_N_ESTIMATORS`, `XGBOOST_MAX_DEPTH`, `XGBOOST_LEARNING_RATE`, `XGBOOST_SUBSAMPLE`, `XGBOOST_COLSAMPLE_BYTREE`, `XGBOOST_VERBOSITY`
- Функции `save_hyperparams` / `load_hyperparams` / `get_default_hyperparams`
- Директория `outputs/{MODEL_VERSION}/hyperparams/` для хранения оптимальных параметров по тикеру

---

## [v11 / v11fix / v11fix2] — Optuna + новые индикаторы

**Файлы:** `stock_modelv11.py`, `stock_modelv11fix.py`, `stock_modelv11fix2.py`

### Добавлено

- Optuna для автоматической оптимизации гиперпараметров (TPE, ~20 trials): LSTM — `units`, `dropout`, `learning_rate`; XGBoost — `n_estimators`, `max_depth`, `learning_rate`, `subsample`, `colsample_bytree`
- Новые технические индикаторы (v11fix): ATR_14, Stoch_K / Stoch_D, ADX_14, Momentum_10, Price_Change_1 / Price_Change_5

### Исправлено (v11fix)

- Feature Importance показывал нули — исправлены именование и агрегация по временным шагам
- Дублирование прогнозов при нескольких горизонтах — отдельный `temp_df` на каждый горизонт
- Архитектура LSTM: 3 слоя (`units → units/2 → units/2`) вместо 2 одинаковых; добавлен Dense+ReLU перед выходом
- Мета-лёрнер XGBoost: 100 деревьев / depth=6 / lr=0.03 (было 50 / 5 / 0.05)
- Ожидаемый прирост: R² 0.52 → 0.65–0.75, RMSE/MAE −10–20%

---

## [v10 / v10.1 / v10_final] — TinkoffFundamentalLoader + резервная загрузка

**Файлы:** `stock_modelv10.py`, `stock_modelv10_final.py`, `stock_modelv10_tinkoff_fundamentals.py`

### Добавлено

- Класс `TinkoffFundamentalLoader` — загрузка фундаментальных данных через REST API Tinkoff Invest: поиск `asset_uid` по тикеру, запрос `GetAssetFundamentals` (P/E, P/B, ROE, Beta, Market Cap, Dividend Yield)
- Резервная загрузка исторических данных: moexalgo → MOEX ISS CSV API с пагинацией
- Структурированное логирование с временными метками (файл + консоль)
- Сохранение и загрузка оптимальных гиперпараметров по тикеру между запусками

---

## [v9] — улучшение стабильности v8

**Файл:** `stock_modelv9.py`

### Изменения

- Более устойчивая загрузка данных с fallback-механизмами
- Улучшенная обработка ошибок при расчёте технических индикаторов
- Расширенное логирование прогресса обучения
- Архитектура ансамбля без изменений (как в v8)

---

## [v8 / v8.1Clean] — XGBoost вместо Random Forest + MACD + Bollinger Bands — 8 января 2026

**Файлы:** `stock_modelv8.py`, `stock_modelv8.1Clean.py`

### Добавлено

- XGBoost заменяет Random Forest на обоих уровнях ансамбля: уровень 0 (base learner) — XGBoost (100 деревьев, max_depth=7, lr=0.1, subsample=0.8); уровень 1 (meta-learner) — XGBoost (50 деревьев, max_depth=5) вместо линейной регрессии
- Новые технические индикаторы: MACD (EMA12 − EMA26), MACD Signal (EMA9), MACD Histogram; Bollinger Bands (Upper / Middle / Lower / Width)
- Количество признаков: 32 (было 24)
- Встроенная регуляризация L1/L2 XGBoost, обработка пропусков

---

## [v7] — абстракция провайдеров данных (демонстрационная версия)

**Файл:** `stock_modelv7.py`

### Добавлено

- Абстрактные провайдеры данных: `MoexISSProvider` (кэш Parquet/CSV, retry с exponential backoff), `TinkoffProvider` (REST API), `TinkoffGRPCProvider` (официальный Tinkoff Invest SDK, gRPC)
- Переключение через `.env`: `TINVEST_USE_GRPC=true/false`
- Версия предназначена для учебных и демонстрационных целей, не для production

---

## [v6] — production-основа: .env, EarlyStopping, стекинг — 8 января 2026

**Файл:** `stock_modelv6.py`

### Добавлено

- Конфигурация через `.env`: токены, `EPOCHS`, `PATIENCE`
- Callbacks Keras: `EarlyStopping` (patience=3), `ModelCheckpoint`, `TensorBoard`
- История обучения сохраняется как JSON
- Структура директорий вывода: `models/`, `logs/`, `graphs/`
- Random Forest (GridSearchCV, 3-fold CV) как второй base learner
- Линейная регрессия как мета-лёрнер (объединяет LSTM + RF)
- 24 финансовых признака

---

## [v4 / v5] — расширение фундаментального анализа — 15 мая 2025

**Файлы:** `stock_modelv4.py`, `stock_modelv5.py`

### Добавлено

- 20+ новых фундаментальных показателей: P/E, P/B, P/S, P/CF, ROA, ROE, Debt/Equity, Current Ratio, Gross Profit Margin, Dividend Yield, EPS Growth, Sales Growth, Operating Margin, Net Profit Margin
- Провайдеры данных: T-Invest API (REST) и FinanceMarker.ru API
- v5: курс USD/RUB из MOEX ISS + интерполяция через scipy

### Исправлено

- v4: несовместимость `moexalgo` с Python 3.10 — добавлен `v4_fixed.py` с расширенным логированием
- v4: токены были захардкожены — переход на переменные окружения

---

## [v3] — мульти-горизонтное прогнозирование + RSI — 14 мая 2025

**Файл:** `stock_modelv3.py`

### Добавлено

- Горизонты прогноза: 2 / 5 / 10 дней (выбор пользователя), убран годовой прогноз
- Технический индикатор RSI (14 периодов)

### Удалено

- Внешние макро-факторы (USD/RUB, цена нефти BRENT) — фокус на фундаментале самой акции

---

## [v1 / v2] — прототип — 2025

**Файлы:** `stock_model.py`, `stock_modelv2.py`

### Возможности

- Простая двухслойная LSTM (2×50 нейронов) + Dense выход
- Горизонт прогноза: 1 год (признано неточным)
- Источники данных: `moexalgo`, `yfinance`, ЦБ РФ (USD/RUB, BRENT)
- Без ансамблирования, без технических индикаторов

---

## Сводная таблица версий

<!-- markdownlint-disable MD060 -->
| Характеристика | v1–v2 | v3 | v4–v5 | v6 | v8 | v10 | v11 | v12–v13 | v14+ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Слоёв LSTM | 2×50 | 2×50 | 2×50 | 2×50 | 2×50 | 2×50 | 3 слоя | 3 слоя | 3 слоя |
| Base learner 2 | — | — | — | RF | XGB | XGB | XGB | XGB | XGB |
| Мета-лёрнер | — | — | — | LR | XGB | XGB | XGB | XGB | XGB |
| Кол-во признаков | 5 | 5 | 24 | 24 | 32 | 32 | 32+ | 32+ | 34 |
| MACD / BB | Нет | Нет | Нет | Нет | Да | Да | Да | Да | Да |
| Tinkoff API | Нет | Нет | Да | Да | Да | Да | Да | Да | Да |
| Optuna | Нет | Нет | Нет | Нет | Нет | Нет | Да | Да | Да |
| look_back | 20 | 20 | 20 | 20 | 20 | 20 | 30 | 30 | 30 |
| .env конфиг | Нет | Нет | Частично | Да | Да | Да | Да | Полный | config.py |
| Горизонт прогноза | 365 д | 2/5/10 д | 2/5/10 д | 2/5/10 д | 2/5/10 д | 2/5/10 д | 2/5/10 д | 2/5/10 д | +1/+2/+3 д |
| Стратегия прогноза | — | — | — | — | авторегрессия | авторегрессия | авторегрессия | авторегрессия | MIMO |
<!-- markdownlint-enable MD060 -->
