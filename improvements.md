# Анализ проекта: плюсы, минусы, рекомендации

<!-- markdownlint-disable MD029 MD036 -->

## Плюсы текущего решения

- **Стэкинг-ансамбль LSTM + XGBoost** — LSTM улавливает временные зависимости; XGBoost устойчив к выбросам. Meta-Learner объединяет их оптимально.
- **Квантильная регрессия для CI** — три квантильные модели (α=0.05, 0.50, 0.95) дают статистически обоснованные доверительные интервалы на основе OOS-остатков.
- **Walk-forward валидация** — правильный способ кросс-валидации для временных рядов, предотвращает data leakage.
- **MIMO (Direct Multi-Step Forecasting)** — отдельная модель на каждый горизонт, нет накопления ошибки авторегрессии.
- **Bayesian hyperparameter search (Optuna)** — персистентность результатов в JSON, не нужно перезапускать оптимизацию.
- **Два источника данных** — `moexalgo` основной, прямой MOEX ISS API как fallback.
- **Обогащение фундаментальными данными** — T-Bank Invest API: P/E, P/B, ROE и другие коэффициенты.
- **Два режима запуска** — интерактивный и CLI (для автоматизации).
- **Сохранение всех артефактов** — логи, графики, гиперпараметры, результаты бэктестов.

---

## Открытые задачи

### Производительность

- **GPU memory growth** — без `tf.config.experimental.set_memory_growth(gpu, True)` TensorFlow занимает всю видеопамять; при наличии других GPU-процессов возможны OOM.
- **Медленная валидация** — walk-forward обучает 54 модели (18 на горизонт × 3); с Optuna прогон занимает значительное время на CPU.

### Код

- **Мета-learner не оптимизируется через Optuna** — гиперпараметры захардкожены в `config.py`; базовые модели оптимизируются, мета-слой нет.
- **Дата начала данных захардкожена** — `start_date = '2014-01-01'` не вынесена в `.env` или CLI; для тикеров с коротким листингом грузится пустой период в начале.
- **Логирование сырого ответа API** — `logger.info(f"RAW API RESPONSE: {resp.text[:500]}...")` пишет первые 500 символов; ответ может содержать чувствительные данные.
- **`value_usd` — константный признак** — `get_usd_rub_rate()` присваивает текущий курс ЦБ РФ всем строкам датафрейма; нулевая дисперсия = модель не может учиться на нём. Либо использовать исторические дневные ставки USD/RUB, либо убрать признак.
- **Фундаментальные признаки — только snapshot** — `market_cap`, `roe`, `pe_ratio` и другие берутся как текущие значения и продублированы на всю историю; модель не видит их динамику. Либо подключить исторические данные (financemarker.ru), либо принять ограничение осознанно.

### Что можно убрать

| Что | Где | Причина |
| --- | --- | --- |
| `FINANCEMARKER_TOKEN` | `.env` | Источник данных не подключён |
| Логирование `RAW API RESPONSE` | `stock_modelv14.py` | Шум + потенциальная утечка данных |

### Что можно добавить

- **GPU memory growth** — добавить в инициализацию: `for gpu in tf.config.list_physical_devices('GPU'): tf.config.experimental.set_memory_growth(gpu, True)`
- **Configurable start date** — `DATA_START_DATE` в `.env` и `--start-date` в CLI
- **Оптимизация мета-learner'а** — включить в Optuna или хотя бы вынести параметры в `config.py`
- **Настраиваемый горизонт прогноза** — через `.env` или CLI (сейчас жёстко `[1, 2, 3]`)
- **Прогресс-бар для Optuna** — `show_progress_bar=True` или tqdm

### Рефакторинг (кандидаты на вынос из stock_modelv14.py)

| Файл | Функции | Причина |
| --- | --- | --- |
| `optuna_tuning.py` | `optimize_lstm_params`, `optimize_xgboost_params` | Полностью изолированы от пайплайна; Optuna-код меняется независимо |
| `data_loader.py` | `load_stock_data_moex_test` | Единственное место для замены источника данных (MOEX → другой) |

Не трогать пока:

- `run_backtest` — плотно завязан на локальные переменные и logger
- `prepare_and_train_model` — громоздкая сигнатура при выносе, мало выгоды

### Из сравнения с ARIMAX

> Аналог — [Stock-Forecast-ARIMAX](https://github.com/GorelikovMatvey/Stock-Forecast-ARIMAX): одиночный SARIMAX, параметрические CI, Jupyter-only. Ниже — идеи, которые стоит перенять.

- **Макропеременные как time-varying признаки** — ARIMAX включает исторический USD/RUB по дням, цену Brent, ставку ЦБ РФ. Это одновременно чинит баг `value_usd` (сейчас константа) и добавляет реально информативные сигналы: Brent критичен для LKOH/GAZP, ставка ЦБ влияет на оценки всех бумаг. Источники: ЦБ РФ XML API (USD/RUB история), CBR key rate, EIA/Yahoo Finance (Brent).
- **Диагностика остатков — тест Льюнга-Бокса** — проверяет, нет ли автокорреляции в OOS-остатках walk-forward; если есть — модель не извлекла всю информацию из ряда. `statsmodels.stats.diagnostic.acorr_ljungbox` после walk-forward цикла. Один вызов, но весомый аргумент на защите.
- **Тест Грэнжера для отбора признаков** — ARIMAX формально проверяет, Granger-причиняет ли признак целевую переменную. Запустить `statsmodels.tsa.stattools.grangercausalitytests` на топ-признаках из XGBoost feature importance — формальное обоснование выбора фичей для диплома.

### Открытые вопросы

1. **GUI / параметры Optuna** — если для тикера нет сохранённых параметров, по умолчанию запускать оптимизацию.
2. **GPU использование** — статус GPU пишется только в лог-файл (`GPU Available: [...]`, строка 87), но не в stdout; неясно, используется ли GPU на самом деле. XGBoost управляется через `XGBOOST_DEVICE='cuda'` в `.env`. LSTM на Windows + TF 2.11+ не имеет CUDA-сборки, GPU недоступен; Optuna сам GPU не требует. Задача: либо вывести GPU-статус в stdout, либо задокументировать ограничения.
3. **Способ демонстрации** — нативный Windows-интерфейс или небольшой веб-сайт для защиты.
4. **Масштаб сохранения графиков** — отдельные срезы за 2 недели и месяц; дополнительный график с ошибкой в RUB и %.
5. **Завершение программы** — завершать работу, не ожидая закрытия matplotlib-окон.
6. **Визуальный разрыв на графике** — matplotlib использует непрерывную временну́ю ось, поэтому выходные и праздники между последним реальным днём и первым прогнозным занимают место и создают видимый разрыв. Расчёт дат корректен, это чисто графический баг. Фикс: бизнес-день ось (`mdates` + `WeekdayLocator`) или категориальные метки.

---

## Архив (реализовано)

### Критические ошибки

- **✅ v13.2 — Ошибка отступа в точке входа** — `if backtest_mode:` / `else:` находились вне `if __name__ == '__main__':`.
- **✅ v13.5 — SSL-верификация отключена** — оба вызова Tinkoff Invest API переведены на `verify=certifi.where()`; добавлен `import certifi`.
- **✅ v13.2 — XGBoost Optuna обучался на train-данных** — добавлен holdout-split 80/20 внутри функции оптимизации.
- **✅ v14.3/v14.5 — CI-bounds систематически ниже точечного прогноза** — квантильные модели теперь обучаются на OOS-остатках walk-forward тестов; CI добавляется как смещение к прогнозу (`lower = pred + residual_q`).

### Качество модели

- **✅ v13.3 — Большинство фундаментальных признаков = 0** — нулевые фичи закомментированы, вектор сокращён с 39 до 29 признаков.
- **✅ v13.2 — Курс USD/RUB захардкожен** — заменён `get_usd_rub_rate()` с XML API ЦБ РФ; fallback 90.0.
- **✅ v14.0 — Наивная авторегрессия для прогноза 2–3 дней** — заменена Direct Multi-Step Forecasting (MIMO): отдельная модель на каждый горизонт.
- **✅ v13.6 — Нет признаков на основе доходностей** — добавлены `Vol_Return_5/10/20` и `Return_MA_5/10`; вектор расширен до 34 признаков.
- **✅ v13.3 — Прогнозные даты не учитывали торговый календарь** — добавлена `next_business_day()`.
- **✅ v14.1 — Прогноз начинался с пропуском текущего дня** — `base_date = data['Date'].max()` вместо `end_date`.

### Код и зависимости

- **✅ v13.4 — Неиспользуемые импорты** — удалены `TensorBoard`, `ModelCheckpoint`, `interp1d`, `io`, `Path`.
- **✅ v14.1 — Мёртвый код `update_technical_indicators_single_row`** — удалён после перехода на MIMO.
- **✅ v13.5 — `requirements.txt`** — зафиксированы 11 прямых зависимостей с точными версиями.

### Вынесенные модули

- **✅ `config.py`** — все гиперпараметры и константы вынесены из основного скрипта.
- **✅ `presentation_output.py`** — презентационный график для слайдов (16:9, русские подписи, 90% CI, PNG 1920×1080 при 150 DPI); вызывается из `stock_modelv14.py` при вопросе 6 интерактивного режима или флаге `--presentation`.

### Прочее

- **✅ Режим воспроизводимого бенчмарка** — `benchmark_mode` в интерактивном меню; фиксированный тикер и дата, дефолтные гиперпараметры, результаты в `benchmarks.md`. Дата старта данных вынесена в `BENCHMARK_START_DATE`.
- **✅ `TINVEST_USE_GRPC`** — переменная удалена из `.env` (использовалась только в legacy gRPC-клиенте, в v14 не нужна).
- **✅ Сравнение с ARIMAX-аналогом** — [Stock-Forecast-ARIMAX](https://github.com/GorelikovMatvey/Stock-Forecast-ARIMAX) изучен; выводы перенесены в подраздел "Из сравнения с ARIMAX".
